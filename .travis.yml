language: java
jdk:
  - oraclejdk8
  - openjdk7
cache:
  directories:
  - "$HOME/.cache"
before_install:
  - cp .travis.settings.xml $HOME/.m2/settings.xml
deploy:
  provider: script
  script: "mvn deploy"
  skip_cleanup: true
